import { ErrorPage } from '@/components/error';

// TODO: This page contains a view of all studies that are currently recruiting
// For each study, there will be a short description and button to candidate

export default async function Postuler() {
    return ErrorPage({ title: "La page n'a pas encore été créée." });
}
